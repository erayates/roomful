{
  "name": "@roomful/react",
  "description": "React provider and hooks for Roomful — realtime presence, cursors, shared state, viewport, locking, pointer, comments, and history.",
  "version": "1.1.1",
  "private": false,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=20"
  },
  "sideEffects": false,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --max-warnings 0",
    "test": "vitest run --config ../../vitest.package.config.ts",
    "test:types": "tsd --files test-d/**/*.test-d.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rimraf dist coverage",
    "test:watch": "vitest --config ../../vitest.package.config.ts"
  },
  "dependencies": {
    "@roomful/core": "workspace:*"
  },
  "devDependencies": {
    "@testing-library/dom": "^10.4.1",
    "@testing-library/react": "^16.3.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "jsdom": "^26.0.0",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "peerDependencies": {
    "react": ">=18.0.0",
    "react-dom": ">=18.0.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
