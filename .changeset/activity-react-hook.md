---
'@roomful/react': minor
---

Add the `useActivity` hook (EP-15): subscribes to the room activity feed and returns it newest-first
(reactive, referentially stable) plus a `record(type, data?)` function. See
`docs/reference/engines-activity.md`.
