---
'@roomful/vue': minor
---

Add the `useActivity` binding (EP-15): exposes a readonly ref to the room activity feed
(newest-first, reactive, referentially stable) plus a `record(type, data?)` control. See
`docs/reference/engines-activity.md`.
