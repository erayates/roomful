---
'@roomful/react': minor
---

Add the `useSessionSummarizer` hook (alpha). It summarizes the current room session from the activity feed and recomputes as the feed changes, returning the reactive `SessionSummary`. Pass a `narrate` hook to render the summary text with an LLM.
