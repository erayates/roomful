---
'@roomful/solid': minor
---

Add the `useActivity` hook (EP-15): returns an accessor for the room activity feed (newest-first,
reactive, referentially stable) plus a `record(type, data?)` control. See
`docs/reference/engines-activity.md`.
